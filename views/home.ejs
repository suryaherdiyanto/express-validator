<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Testing Home</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

    <div class="container">
        <form action="/validation-form" method="POST">
            <div class="form-group">
              <label for="exampleInputEmail1">Name</label>
              <input type="text" class="form-control" name="name" id="exampleInputEmail1" placeholder="Enter your name">
              <% if(validationErrors) { %>
                <% if(validationErrors.name) { %>
                    <span class="badge badge-danger"><%= validationErrors.name[0] %></span>
                <% } %>
              <% } %>
            </div>
            <div class="form-group">
              <label for="exampleInputAge1">Age</label>
              <input type="number" class="form-control" name="age" id="exampleInputPassword1" placeholder="Age">
              <% if(validationErrors) { %>
                <% if(validationErrors.age) { %>
                    <span class="badge badge-danger"><%= validationErrors.age[0] %></span>
                <% } %>
              <% } %>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
    <script src="../index.js"></script>
</body>
</html>